let states = {
  name: 'Y 2 Care',
  search: 'One Search'
}

function rootReducer(state = states, action) {
    switch (action.type) {
        default:
            return state;
    }
}

export default rootReducer;
